version: '2'
services:
  boinc:
    restart: always
    build: ./
    volumes:
      - "boinc_data:/data:rw"
    environment:
      PROJECT_URL: 
      AUTH_KEY: 
volumes:
  boinc_data:
    driver: local
